import React from 'react';
import PropTypes from 'prop-types';

Courses.propTypes = {
    
};

function Courses(props) {
    return (
        <div>
            <p>courses</p>
        </div>
    );
}

export default Courses;